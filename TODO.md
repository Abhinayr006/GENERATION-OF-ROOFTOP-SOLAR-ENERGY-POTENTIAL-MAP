# TODO: Upload Project to GitHub excluding venvs

## Steps:
1. ✅ Create .gitignore excluding .venv/, venv/, __pycache__, *.pyc
2. Run `git init`
3. Run `git add .`
4. Run `git commit -m "Initial commit: Upload project files including DataSet excluding venvs"`
5. Run `git remote add origin https://github.com/Abhinayr006/GENERATION-OF-ROOFTOP-SOLAR-ENERGY-POTENTIAL-MAP.git`
6. Run `git branch -M main`
7. Run `git push -u origin main`
8. Verify with `git status` and `git log --oneline`
9. Test app: `source venv/bin/activate && python3 app.py`
10. attempt_completion

Progress: Starting step 1.
